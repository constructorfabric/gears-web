# UI Kit Demo (kitchen sink)

A Vite app showing every registry component in every state (variants, sizes,
disabled, errors, light/dark). Built in CI as a live smoke test.

Placeholder — the app lands together with the first batch of components.
