# AI layer

Agent-facing assets for the UI Kit:

- `llms.txt` — map of the kit for agents: component list with links to usage docs.
- Usage rules per component (also embedded in registry item metadata for MCP).

Placeholder — lands together with the components. Consumers connect the official
shadcn MCP server; see QUICKSTART.
