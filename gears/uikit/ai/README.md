# AI layer

Agent-facing assets for the UI Kit:

- `llms.txt` — map of the kit for agents: component list with links to usage
  docs. Shipped inside the npm package so agents find it in `node_modules`.
- Usage rules per component: when to use it, anti-patterns, composition examples.

Placeholder — lands together with the components.
