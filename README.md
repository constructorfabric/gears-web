# Gears Web

This repo is Gears Web.

## Contributing

Pull requests require a `Signed-off-by` trailer (DCO). Commit with `git commit -s`.
Reviewers are assigned automatically from `.github/CODEOWNERS`.
